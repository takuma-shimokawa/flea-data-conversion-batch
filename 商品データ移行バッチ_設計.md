# 商品データ移行バッチ 設計まとめ

## 1. 目的

`original` テーブルにある旧商品データを、Spring Batchを使用して以下へ移行する。

* `category` テーブル
* `items` テーブル

カテゴリは文字列で保持されているため、階層構造へ変換して移行する。

---

## 2. バッチ全体構成

```text
Job：商品データ移行
  ↓
Step1：categoryStep
  ↓ 正常終了
Step2：itemsStep
```

`items.category` に `category.id` を設定する必要があるため、**category移行を先に実行する。**

各Stepは大量データを扱うため **Chunk方式** とする。

### チャンクサイズ

初期値：

```text
5,000件
```

性能確認後に変更できるよう、`application.yml` で設定する。

---

## 3. categoryStep

### Reader

`original` テーブルから `category_name` を取得する。

Reader側では、

* `null` を除外
* 空文字を除外
* 同じ `category_name` の重複を減らす
* 3階層未満は除外しない

とする。

### Processor

取得した `category_name` を最大3階層に分割し、
各階層ごとの `Category` を作成する。

例：

```text
Men/Tops/T-shirts

### Writer

親カテゴリから順番に `category` テーブルへ登録する。

```text
Men
↓
Tops
↓
T-shirts
```

登録時に採番されたIDを取得して、次の階層の `parent_id` に使用する。

同じカテゴリを何度もDB検索しないよう、

```text
Men → id
Men/Tops → id
Men/Tops/T-shirts → id
```

のような **カテゴリパスとIDの対応表をメモリ上に保持する。**

すでに存在するカテゴリは再登録せず、保持しているIDを再利用する。

---

## 4. itemsStep

itemsStep開始時に一度 `category` テーブルを読み込み、

```text
Men/Tops/T-shirts → category.id
```

という対応表をメモリ上に作成する。

categoryStepで使っていた情報をそのまま引き継ぐのではなく、**実際にDBへ登録されたcategoryデータを正として読み直す。**

### Reader

`original` から商品1件分を取得する。

主な項目：

```text
id
name
brand
price
shipping
description
condition_id
category_name
```

### Processor

商品データをチェック・加工する。

#### エラー条件

以下の場合はitemsへ移行しない。

* `category_name = null`
* `category_name = 空`
* 3階層未満
* 対応する `category.id` が見つからない

エラー理由は ErrorReason enum で一元管理する。

ItemProcessorでは文字列を直接設定せず、
ErrorReasonの定義値をItemProcessResultへ設定する。

CSV出力時はErrorReasonが保持するcodeを取得して出力する。

ErrorReason：

CATEGORY_NULL
CATEGORY_EMPTY
CATEGORY_LEVEL_INSUFFICIENT
CATEGORY_NOT_FOUND

CSV出力値：

CATEGORY_NULL
CATEGORY_EMPTY
CATEGORY_LEVEL不足
CATEGORY_NOT_FOUND

#### 正常データ

以下は基本的にそのまま移行する。

```text
original.name         → items.name
original.brand        → items.brand
original.price        → items.price
original.shipping     → items.shipping
original.description  → items.description
original.condition_id → items.condition
```

`category_name` は3階層までに整形して、

```text
category.name_all
```

に対応する `category.id` を取得し、

```text
items.category
```

へ設定する。

`items.id` は自動採番。

---

## 5. 正常データ・エラーデータの振り分け

Processorの処理結果を、

```text
ClassifierCompositeItemWriter
```

で振り分ける。

```text
ItemProcessor
      ↓
ClassifierCompositeItemWriter
      ├─ 正常 → ItemWriter
      └─ エラー → ErrorCsvWriter
```

### ItemWriter

正常データを `ItemRepository` 経由で `items` テーブルへ登録する。

### ErrorCsvWriter

エラーデータをCSVへ出力する。

---

## 6. エラーCSV

出力項目は以下とする。

```text
id
name
category_name
error_reason
```

エラーデータは FlatFileItemWriter を使用してCSVへ出力し、ファイルのopen / closeはSpring BatchのStepで管理する。

CSVは再実行時に同じファイルへ追記せず、**バッチ実行ごとに新しいファイルを作成する。**

例：

```text
error_20260824_170000.csv
```

---

## 7. 進捗・処理時間

### ProgressListener

10,000件処理するごとにコンソールへ進捗を表示する。

例：

```text
10,000件処理しました
20,000件処理しました
```

categoryStep / itemsStepで共通利用する。

### ExecutionTimeListener

以下の処理時間を記録する。

* Job全体
* categoryStep
* itemsStep

Job全体の時間を使用して、**10分以内という性能要件を確認する。**

---

## 8. application.yml

以下の設定値をJavaコードから外出しする。

```text
チャンクサイズ
エラーCSV出力パス
エラーCSVファイル名
```

とする。

---

## 9. ロールバック

Chunk方式で実装するため、基本的に**チャンク単位でトランザクション管理**する。

例：

```text
チャンクサイズ：5000

1～5000
→ コミット

5001～10000
→ 途中で例外
↓
このチャンクをロールバック
```

今回はこの方式で進める。

---

## 10. 再実行

バッチ自身が自動で `items` / `category` を削除する処理は持たせない。

再実行する場合は事前に、

```text
items削除
↓
category削除
↓
前回とは異なるJobParameterを指定
↓
バッチ再実行
```

とする。

`items` が `category` を参照するため、削除順に注意する。

---

## 11. 主なクラス構成

```text
BatchConfig

CategoryReader
CategoryProcessor
CategoryWriter
CategoryRepository

ItemReader
ItemProcessor
ItemProcessResult
ItemsWriter
ItemRepository

ErrorReason
ErrorCsvWriter

ProgressListener

ExecutionTimeListener

Original

Category

Item

ErrorCsvProperties
```

`OriginalRepository` は作らず、Spring BatchのReader機能を使って `original` テーブルを直接読み込む。

---

## 12. 処理全体

```text
Job開始
  ↓
categoryStep
  ↓
original.category_nameを取得
  ↓
カテゴリを最大3階層に加工
  ↓
親から順にcategoryへ登録
  ↓
categoryStep終了
  ↓
categoryの「フルパス → ID」をメモリへ読み込み
  ↓
itemsStep
  ↓
originalの商品を読む
  ↓
正常 / エラー判定
  ↓
category.idへ変換
  ↓
振り分け
  ├─ 正常 → itemsへ登録
  └─ エラー → CSV出力
  ↓
Job終了
  ↓
処理時間・件数を確認
```

以上を現時点の**実装前設計**とする。
